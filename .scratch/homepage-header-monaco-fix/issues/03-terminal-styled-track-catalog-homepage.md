# 03 — Terminal-Styled Track Catalog Homepage

**What to build:** Refactor `GET /` to render the **Track Catalog Homepage** formatted with terminal design consistency (monospaced typography, dark/light theme engine integration, command prompt markers `$`, monospaced track progress metrics, and track selection cards).

**Blocked by:** 01 — Header Titlebar Branding Refactor & UI Track Switcher.

**Status:** completed

- [x] `GET /` renders the terminal-styled Track Catalog Homepage.
- [x] Users can view all available language tracks (Go, TypeScript), progress statistics, and module counts.
- [x] Clicking a track or selecting a track from the UI navigates to `/tracks/[trackSlug]`.
- [x] Terminal styling (typography, prompts `$`, dark/light theme mode, monospaced borders) is consistent across the page.
