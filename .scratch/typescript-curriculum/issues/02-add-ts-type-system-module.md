# 02 — Add TypeScript `01-type-system` Module

**What to build:** New 7-chapter module covering interfaces & type aliases, union/intersection/literal types, and utility types (`Partial`, `Pick`, `Omit`, `Record`, `Exclude`, `ReturnType`, etc.).

**Blocked by:** #01

**Status:** done

- [x] `module.json` created — title "The TypeScript Type System", order 1
- [x] `01-interfaces-type-aliases-reading.mdx` — `interface` vs `type`, declaration merging, `extends`, `readonly`, index signatures, structural typing
- [x] `02-interfaces-type-aliases-challenge.mdx` — Product/DiscountedProduct interface design + `finalPrice`
- [x] `03-union-intersection-reading.mdx` — unions, literal types, intersections, discriminated unions in depth, `keyof`, index access types
- [x] `04-union-intersection-challenge.mdx` — `unwrap` (Result pattern) + `combine` (intersection) + `getConfigValue` (keyof constraint)
- [x] `05-utility-types-reading.mdx` — `Partial`, `Required`, `Readonly`, `Pick`, `Omit`, `Record`, `Exclude`, `Extract`, `NonNullable`, `ReturnType`, `Parameters`
- [x] `06-utility-types-challenge.mdx` — `createPatch`, `toPublicProfile`, `buildRoleMap`, `withoutEmail` using UserProfile
- [x] `07-type-system-assessment.mdx` — Typed Event Bus capstone (`emit`, `dispatch`, `summarize`)
