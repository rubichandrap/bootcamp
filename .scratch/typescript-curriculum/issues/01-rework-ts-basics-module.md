# 01 — Rework TypeScript `00-basics` Module

**What to build:** Replace the thin hello-world-only `00-basics` module with a full 7-chapter module covering TypeScript program structure, type annotations, variables & type inference, and control flow with narrowing.

**Blocked by:** None

**Status:** done

- [x] `01-hello-world-reading.mdx` reworked — covers program structure, type annotations, inference, primitive types table
- [x] `02-hello-world-challenge.mdx` replaced — type annotation challenge (add, repeat, isEven)
- [x] `03-variables-and-types-reading.mdx` added — `let`/`const` widening, arrays, tuples, `any` vs `unknown`, `typeof`, `never`
- [x] `04-variables-and-types-challenge.mdx` added — `processInput` (unknown narrowing) + `describe` (tuple destructuring)
- [x] `05-control-flow-narrowing-reading.mdx` added — `if`/`else`, `in`, `instanceof`, discriminated unions, exhaustiveness with `never`, truthiness
- [x] `06-control-flow-narrowing-challenge.mdx` added — `area` (discriminated union switch) + `classify` (typeof narrowing)
- [x] `07-basics-assessment.mdx` replaced — Shape Renderer capstone integrating all three topics
- [x] Old trivial `03-basics-assessment.mdx` removed
