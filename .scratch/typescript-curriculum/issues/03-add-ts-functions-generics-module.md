# 03 — Add TypeScript `02-functions-and-generics` Module

**What to build:** New 7-chapter module covering function overloads, generics & type parameters, generic constraints, mapped types, and conditional types.

**Blocked by:** #02

**Status:** done

- [x] `module.json` created — title "Functions & Generics", order 2
- [x] `01-function-signatures-reading.mdx` — optional/default params, rest params, function types, overloads, `this` typing, `never` return
- [x] `02-function-signatures-challenge.mdx` — `parse` overload (string↔number) + `pipe` (2-step and 3-step overloads)
- [x] `03-generics-reading.mdx` — generic functions, generic arrays, multiple type params, constraints (`extends`), `keyof` constraints, default type params
- [x] `04-generics-challenge.mdx` — `zip`, `groupBy`, `pluck` using generic constraints
- [x] `05-mapped-types-reading.mdx` — conditional types, `infer`, mapped types, modifier removal (`-readonly`, `-?`), key remapping (`as`), template literal types, `DeepReadonly`
- [x] `06-mapped-types-challenge.mdx` — `Nullable<T>` mapped type + `toNullable` + `fromEntries`
- [x] `07-functions-generics-assessment.mdx` — Generic Data Pipeline capstone (`pipeline` overloads, `filterMap`, `mapRecord`)
