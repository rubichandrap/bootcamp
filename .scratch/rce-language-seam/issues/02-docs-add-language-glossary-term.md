# 02 — docs(domain): add "Language" glossary term

**What to build:** The domain glossary defines a canonical **Language** term — the programming language a Track teaches and the RCE Engine executes (Go, TypeScript, Python, ...) — distinct from a Track, which is the curriculum built around that language. `track.json.language` and `TrackConfig.language` are the canonical source.

**Blocked by:** None — can start immediately.

**Status:** done

- [x] CONTEXT.md defines "Language" with a canonical source and Avoid terms.
- [x] Existing "Track" glossary entry cross-referenced so the Track/Language distinction is explicit.
