---
title: "Slice Operations & Filtering Challenge"
type: "challenge"
order: 4
starterCode: |
  package main

  // FilterEvens returns a new slice containing only even integers from numbers.
  func FilterEvens(numbers []int) []int {
  	// TODO: Iterate over numbers, append evens to new slice, return result
  	return nil
  }

  // SliceSum returns the sum of all elements in numbers using range loop.
  func SliceSum(numbers []int) int {
  	// TODO: Calculate sum using for range
  	return 0
  }
testCode: |
  package main

  import "testing"

  func TestFilterEvens(t *testing.T) {
  	nums := []int{1, 2, 3, 4, 5, 6}
  	got := FilterEvens(nums)
  	want := []int{2, 4, 6}
  	if len(got) != len(want) {
  		t.Fatalf("expected len %d, got %d", len(want), len(got))
  	}
  	for i, v := range want {
  		if got[i] != v {
  			t.Errorf("at index %d: expected %d, got %d", i, v, got[i])
  		}
  	}
  }

  func TestSliceSum(t *testing.T) {
  	nums := []int{10, 20, 30}
  	got := SliceSum(nums)
  	if got != 60 {
  		t.Errorf("expected sum 60, got %d", got)
  	}
  }
---

# Slice Operations & Filtering Challenge

Practice `append`, `range` iteration, and slice creation!

## Tasks

1. Implement `FilterEvens(numbers []int) []int` to return a slice of even numbers.
2. Implement `SliceSum(numbers []int) int` using `for range`.
