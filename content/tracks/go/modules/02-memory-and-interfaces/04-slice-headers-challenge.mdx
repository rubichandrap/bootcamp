---
title: "Slice Header Append Challenge"
type: "challenge"
order: 4
starterCode: |
  package main

  // AppendInt appends value to slice and returns the updated slice.
  func AppendInt(slice []int, val int) []int {
  	// TODO: Implement append logic
  	return nil
  }
testCode: |
  package main

  import "testing"

  func TestAppendInt(t *testing.T) {
  	s := []int{10, 20}
  	s = AppendInt(s, 30)
  	if len(s) != 3 || s[0] != 10 || s[1] != 20 || s[2] != 30 {
  		t.Errorf("expected [10 20 30], got %v", s)
  	}
  }
---

# Slice Header Append Challenge

Reinforce slice header updates with `append`!

## Tasks

Implement `AppendInt(slice []int, val int) []int` to append `val` to `slice` and return the updated slice header.
