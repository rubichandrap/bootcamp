---
title: "Hello World Challenge"
type: "challenge"
order: 2
starterCode: "export function greet(name: string): string {\n  // Implement greet\n  return '';\n}"
testCode: "import { describe, it, expect } from 'vitest';\nimport { greet } from './solution';\n\ndescribe('greet', () => {\n  it('returns greeting', () => {\n    expect(greet('World')).toBe('Hello, World!');\n  });\n});"
---

# Challenge: Hello World

Implement the `greet` function so that it returns `"Hello, <name>!"`.
