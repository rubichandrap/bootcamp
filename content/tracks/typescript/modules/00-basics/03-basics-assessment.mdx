---
title: "TypeScript Basics Assessment"
type: "assessment"
order: 3
starterCode: "export function formatUser(name: string, age: number): string {\n  // Implement formatUser\n  return '';\n}"
testCode: "import { describe, it, expect } from 'vitest';\nimport { formatUser } from './solution';\n\ndescribe('formatUser Assessment', () => {\n  it('formats user name and age', () => {\n    expect(formatUser('Alice', 30)).toBe('Alice (30)');\n  });\n});"
---

# Assessment: TypeScript Basics

Integrate type annotations and function signatures to implement `formatUser(name: string, age: number): string` returning `"Name (Age)"`.
