---
title: "Promises Challenge"
type: "challenge"
order: 2
starterCode: |
  // delay returns a Promise that resolves with the given value after `ms` milliseconds.
  export function delay<T>(value: T, ms: number): Promise<T> {
    // TODO: use setTimeout inside a new Promise
    return Promise.resolve(value);
  }

  // withDefault wraps a Promise: if it rejects, resolve with the defaultValue instead.
  export function withDefault<T>(promise: Promise<T>, defaultValue: T): Promise<T> {
    // TODO: use .catch to return defaultValue on rejection
    return promise;
  }

  // transformAll takes an array of Promises and a mapper function.
  // Returns a Promise that resolves with all values transformed.
  export function transformAll<T, U>(
    promises: Promise<T>[],
    mapper: (value: T) => U
  ): Promise<U[]> {
    // TODO: use Promise.all + .then to map resolved values
    return Promise.resolve([]);
  }
testCode: |
  import { describe, it, expect } from 'vitest';
  import { delay, withDefault, transformAll } from './solution';

  describe('delay', () => {
    it('resolves with the value after the given ms', async () => {
      const result = await delay(42, 10);
      expect(result).toBe(42);
    });
    it('works with strings', async () => {
      const result = await delay('hello', 5);
      expect(result).toBe('hello');
    });
  });

  describe('withDefault', () => {
    it('returns resolved value when promise succeeds', async () => {
      const result = await withDefault(Promise.resolve(99), 0);
      expect(result).toBe(99);
    });
    it('returns defaultValue when promise rejects', async () => {
      const result = await withDefault(Promise.reject(new Error('oops')), -1);
      expect(result).toBe(-1);
    });
  });

  describe('transformAll', () => {
    it('resolves and maps all promises', async () => {
      const ps = [Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)];
      const result = await transformAll(ps, (n) => n * 10);
      expect(result).toEqual([10, 20, 30]);
    });
  });
---

# Challenge: Promises

Implement `delay`, `withDefault`, and `transformAll` using Promise constructor, `.catch`, and `Promise.all`.
